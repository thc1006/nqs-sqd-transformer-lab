molecule:
  name: H2
  bond_length: 0.74

encoding:
  bits: 12

sampling:
  n_samples: 256
  sampler: transformer

nqs:
  d_model: 128
  n_heads: 4
  n_layers: 2
  dropout: 0.0
  learning_rate: 1.0e-4
  n_epochs: 20

sqd:
  max_subspace_dim: 256
